/**
 * Rooms API
 */

/* eslint-disable */

const getters = {
}

const setters = {
  // Create a new room
  /*async createRoom (data) {
    await db.collection('rooms').add(data)
      .then(ref => {
        console.log('(db.rooms.createRoom) Room with id [' + ref.id + '] created.')
      })
      .catch(e => {
        console.log('(db.rooms.createRoom) Error: (' + e.code + ') ' + e.message)
        throw { code: e.code, message: e.message }
      })
  }*/
}

// Combine the modules
export default {
  hey: 'why no work?'
}
